現在はひらがなだけを練習できる学習アプリですが、ホーム画面の「学習モード」を押した後に「ひらがな」または「カタカナ」を選択できるように変更してください。

## 変更前

```text
ホーム画面
  ↓
学習モード
  ↓
ひらがなの書き練習
```

## 変更後

```text
ホーム画面
  ↓
学習モード
  ↓
文字の種類を選択
  ├─ ひらがな
  └─ カタカナ
       ↓
選択した文字の書き練習
```

## 新しい選択画面

ホーム画面の「学習モード」を押したら、すぐ書き練習を開始せず、文字の種類を選ぶ画面を表示してください。

画面には次を表示してください。

- タイトル：「文字を選んでください」
- 「ひらがな」ボタン
- 「カタカナ」ボタン
- ホームへ戻る操作

選択後の動作：

- 「ひらがな」→ひらがなの書き練習を開始
- 「カタカナ」→カタカナの書き練習を開始

Androidスマートフォンで押しやすい、大きな選択ボタンにしてください。

## 関連ファイル

- @src/App.tsx
- @src/pages/HomePage.tsx
- @src/pages/StudyPage.tsx
- @src/pages/ProgressPage.tsx
- @src/data/hiragana.ts
- @src/types/index.ts
- @src/store/LearningContext.tsx
- @src/services/storage.ts
- @README.md

必要であれば、新しい選択画面やカタカナデータ用のファイルを追加してください。

## カタカナの対象文字

初期版では、ひらがなと同じ清音46文字を追加してください。

```text
ア、イ、ウ、エ、オ
カ、キ、ク、ケ、コ
サ、シ、ス、セ、ソ
タ、チ、ツ、テ、ト
ナ、ニ、ヌ、ネ、ノ
ハ、ヒ、フ、ヘ、ホ
マ、ミ、ム、メ、モ
ヤ、ユ、ヨ
ラ、リ、ル、レ、ロ
ワ、ヲ、ン
```

濁音、半濁音、拗音は今回追加しないでください。

## 文字データの設計

ひらがなとカタカナで共通の型を使用できるようにしてください。

各文字に、文字種を識別する情報を追加してください。

例：

```ts
type CharacterScript = 'hiragana' | 'katakana'

interface CharacterData {
  id: string
  character: string
  romaji: string
  row: string
  script: CharacterScript
}
```

既存コードとの整合性を考慮し、実際のプロパティ名は現在の設計に合わせてください。

## IDの重複防止

ひらがなとカタカナで同じIDを使用しないでください。

ただし、既存のひらがなの学習記録を壊さないことを優先してください。

推奨方法：

- 既存のひらがなIDは変更しない
- カタカナIDだけ`katakana-a`、`katakana-ka`のように区別する

例：

```text
ひらがなの「あ」：a
カタカナの「ア」：katakana-a
```

IDを変更する場合は、既存のLocalStorageデータを確実に移行してください。

## 書き練習

StudyPageは、選択された文字種に対応する文字だけを表示してください。

ひらがなを選んだ場合：

```text
あ → い → う → え → お → か → …
```

カタカナを選んだ場合：

```text
ア → イ → ウ → エ → オ → カ → …
```

次の機能は、ひらがなとカタカナの両方で同じように動作させてください。

- 見本文字の表示
- Canvasへの書き込み
- 「書き直す」
- 5回書いた後に「次へ」を有効にする
- 「次へ」を押したら確認画面なしで次の文字へ進む
- 練習回数の記録
- 最後の文字での学習完了
- PCとスマートフォンでの描画

## 選択状態の扱い

画面遷移時に、選択した文字種を明示的に渡してください。

例：

```ts
navigate('/study', {
  state: { script: 'katakana' },
})
```

ただし、ページを再読み込みした場合にもエラーにならないようにしてください。

文字種が取得できない場合は、選択画面へ戻すか、安全な既定値を使用してください。意図せず常にひらがなへ進む実装にはしないでください。

URLで文字種を表現する方法の方が現在のルーティングに適している場合は、次のような設計でも構いません。

```text
/study/hiragana
/study/katakana
```

保守しやすく、再読み込みに強い方法を選んでください。

## 学習記録

ひらがなとカタカナの学習記録を別々に保存してください。

それぞれの文字について、現在のデータ構造に合わせて次を保持してください。

- 学習回数
- 書いた回数
- 手動の習得済み状態
- 最終学習に必要な情報

カタカナの練習が、同じ読みのひらがなの記録へ加算されないようにしてください。

例：

- 「あ」と「ア」は別の学習記録
- 「か」と「カ」は別の学習記録

## 学習ステータス

ひらがなとカタカナの両方で、現在の学習ステータス仕様を維持してください。

- 未学習：書いた回数が0回で、習得済みに設定されていない
- 学習中：書いた回数が1回以上で、習得済みに設定されていない
- 習得済み：「習得済みにする」ボタンが押されている

習得状態は文字ごとに変更・解除できるようにしてください。

## 学習記録画面

学習記録画面でも、ひらがなとカタカナを切り替えて確認できるようにしてください。

推奨UI：

```text
［ひらがな］［カタカナ］
```

- 選択中の文字種を文字と見た目の両方で示す
- 五十音表には選択中の文字種だけを表示する
- ひらがなとカタカナの進捗を混ぜない
- 各文字から、その文字だけの書き練習へ移動できる
- 幅360pxで横スクロールを発生させない

## LocalStorageの移行

既存利用者のひらがな学習記録を消さないようにしてください。

- データ構造のバージョンを更新する
- 既存のひらがな記録をそのまま引き継ぐ
- カタカナ46文字の空の記録を追加する
- 古いデータにカタカナが存在しなくてもエラーにしない
- データ移行を理由に記録全体を初期化しない
- 学習記録のリセットでは、ひらがなとカタカナの両方をリセットする

## UI条件

- ひらがなとカタカナを間違えにくい表示にする
- 選択肢には実際の文字例を表示してもよい

例：

```text
ひらがな
あ か さ