スマートフォンの書きモードを「一画面完結型」のデザインへ変更してください。

## 現在の問題

- PCでは問題なく操作できる
- スマートフォンではCanvasが画面の大部分を占める
- Canvas上のタッチは描画として扱われるため、ページのスクロールを始めにくい
- 下部の「書き直す」「次の文字」ボタンへ移動しにくい
- Android ChromeとPWAで利用している

## 実現したいデザイン

一般的なAndroidスマートフォンの縦画面では、スクロールせずに次の要素をすべて表示してください。

1. コンパクトなヘッダー
2. 学習の進行状況
3. 現在のひらがなとローマ字
4. 書き練習用Canvas
5. 練習回数
6. 「書き直す」ボタン
7. 「次の文字」または「完了する」ボタン

Canvasの大きさは画面幅だけで決めず、利用可能な画面の高さも考慮して調整してください。

## 関連ファイル

- @src/pages/StudyPage.tsx
- @src/components/WritingCanvas.tsx
- @src/components/Layout.tsx
- @src/components/ProgressBar.tsx
- @src/styles/index.css

必要であれば、これらから参照されているファイルも調査してください。

## レイアウト方針

- 書きモード全体を縦方向のFlexレイアウトにする
- Androidの可変アドレスバーを考慮し、可能なら`100dvh`を利用する
- PWAのスタンドアロン表示も考慮する
- ヘッダーや上下の余白をコンパクトにする
- ひらがな、ローマ字、練習回数を必要以上に縦へ広げない
- Canvasは正方形を維持する
- Canvasは画面幅と残りの高さの小さい方に合わせる
- Canvasに適切な最小・最大サイズを設ける
- 操作ボタンは画面下部に常に見える位置へ配置する
- 「書き直す」と「次の文字」は横2列を基本とする
- 主要ボタンの高さは48px以上を維持する
- Androidのセーフエリアも考慮する
- 幅360pxで横スクロールを発生させない

CSSの`clamp()`、`min()`、`dvh`などを使い、可能な限りCSS中心でレスポンシブにしてください。

端末の高さを固定値で決め打ちしないでください。

## 小さい画面への対応

通常のスマートフォンではスクロール不要にしてください。

ただし、高さが極端に小さい画面や文字サイズを拡大している環境では、要素を切り取ったり重ねたりせず、ページ全体を縦スクロールできるようにしてください。

その場合も次の条件を満たしてください。

- Canvas外の領域から自然にスクロールを開始できる
- ボタンがCanvasに重ならない
- 内容が画面外に完全に隠れない
- アクセシビリティ設定を壊さない

## Canvasの条件

- 指とマウスの両方で書ける
- Canvas上では画面をスクロールさせず、描画を優先する
- Canvas外では通常どおりスクロールできる
- 指の位置と描画位置を一致させる
- 高DPI端末でも見本と手書き線を正しい位置に表示する
- レイアウト変更によって書き練習の回数判定を壊さない
- 端末のアドレスバー表示・非表示による小さなリサイズで、書いている途中の線を消さない
- 画面回転や本当にCanvasサイズが変わった場合も、可能なら既存の手書き線を保持して再描画する
- リサイズのたびに無条件で練習内容を初期化しない

## 変更しないもの

- 書き練習の記録処理
- LocalStorageのデータ
- 学習ステータスの判定
- 五十音データ
- PWA設定
- 他の画面のデザイン
- 音声・テスト機能の削除に関する現在の状態

音声やテスト機能がすでに削除されている場合は、再追加しないでください。

## 確認する画面サイズ

最低限、次のサイズを想定してレイアウトを確認してください。

- 360×640
- 360×800
- 390×844
- PC表示

## 完成条件

- 360×640の縦画面で主要な操作が一画面に収まる
- 通常のAndroid端末でスクロールせず書き練習を完了できる
- Canvas、練習回数、操作ボタンが同時に見える
- Canvas上の描画が正常に動作する
- Canvas外では必要に応じてスクロールできる
- 「書き直す」と「次の文字」が押しやすい
- 画面回転やアドレスバーの変化で手書き線が意図せず消えない
- 幅360pxで横スクロールが発生しない
- PC表示を壊さない
- TypeScriptとビルドのエラーがない
- 未使用importやLintエラーがない

最初に現在の高さの使われ方とスクロールしにくい原因を確認し、修正方針を説明してください。その後、そのまま最小限の差分で実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

最後に次を報告してください。

- 変更したファイル
- Canvasサイズの決定方法
- 一画面へ収めるために削減した余白
- 極端に高さが小さい画面でのフォールバック
- Canvasのリサイズ時に手書き線を保持する方法
- 各画面サイズでの確認結果
- Android実機で確認すべき項目