現在のひらがな学習モードと同じ機能・画面構成を、カタカナ学習モードにも反映してください。

すでにカタカナ機能が実装されている場合は、現在の実装を調査し、今回の仕様と異なる部分を修正してください。別のカタカナ機能を重複して追加しないでください。

## 実現したい画面遷移

```text
ホーム画面
  ↓
「学習モード」
  ↓
文字種選択画面
  ├─ ひらがな
  │    ↓
  │  ひらがな文字一覧
  │    ↓ 文字をタップ
  │  書き練習
  │
  └─ カタカナ
       ↓
     カタカナ文字一覧
       ↓ 文字をタップ
     書き練習
```

「ひらがな」または「カタカナ」を選択した直後に書き練習を開始しないでください。

必ず文字一覧を表示し、利用者が練習したい文字をタップしてから書き練習へ移動するようにしてください。

## 文字種選択画面

ホーム画面の「学習モード」を押したら、次の2つを表示してください。

```text
ひらがな
あ か さ

カタカナ
ア カ サ
```

それぞれを大きく押しやすいボタンまたはカードにしてください。

- 「ひらがな」→ひらがなの文字一覧
- 「カタカナ」→カタカナの文字一覧

主要な選択領域は48px以上の高さにしてください。

## ひらがな文字一覧

現在のひらがな学習モードにある文字一覧、五十音表、学習ステータス、文字選択処理を維持してください。

既存のひらがな機能を作り直したり、動作を変更したりしないでください。

文字をタップしたら、その文字を対象とした書き練習へ移動してください。

## カタカナ文字一覧

ひらがなの文字一覧と同じレイアウト、操作方法、学習ステータス表示を使用してください。

対象文字は清音46文字です。

```text
ア、イ、ウ、エ、オ
カ、キ、ク、ケ、コ
サ、シ、ス、セ、ソ
タ、チ、ツ、テ、ト
ナ、ニ、ヌ、ネ、ノ
ハ、ヒ、フ、ヘ、ホ
マ、ミ、ム、メ、モ
ヤ、ユ、ヨ
ラ、リ、ル、レ、ロ
ワ、ヲ、ン
```

カタカナ文字一覧には、次を表示してください。

- カタカナ46文字
- 各文字の学習ステータス
- 各文字の書いた回数
- 必要に応じて習得済み状態
- タップできることが分かる表示

文字をタップしたら、選択したカタカナの書き練習へ移動してください。

## ひらがなと同じ機能を再利用する

カタカナ専用の処理を大量に複製しないでください。

次の処理は、ひらがなとカタカナで可能な限り共通化してください。

- 文字一覧の表示
- 五十音の行ごとの表示
- 文字タップ処理
- 学習ステータス表示
- 書き練習画面
- Canvasの描画
- 練習回数のカウント
- 「書き直す」
- 5回書いた後の「次へ」
- 習得済み状態の変更
- LocalStorageへの保存

文字データと選択中の文字種を切り替えることで、同じコンポーネントを再利用できる構造にしてください。

## 書き練習

カタカナでも、現在のひらがなと同じ書き練習を行えるようにしてください。

- タップした文字を大きく表示する
- ローマ字を表示する
- Canvasに薄い見本文字を表示する
- 指とマウスで書ける
- 「書き直す」が動作する
- 5回書くまで「次へ」を押せない
- 5回書いたら「次へ」を押せる
- 書いた回数を正しく保存する

ひらがなとカタカナでCanvasの処理を分けないでください。見本文字として渡す文字だけを変更してください。

## 「次へ」の動作

ひらがなの現在の動作をカタカナにもそのまま適用してください。

カタカナの場合は、カタカナの五十音順で次へ進みます。

```text
ア → イ → ウ → エ → オ → カ → キ → …
```

カタカナの練習中に、ひらがなの次の文字へ移動しないようにしてください。

最後の文字「ン」の動作も、ひらがなの「ん」と同じにしてください。

文字一覧から1文字だけ練習する場合の終了後の動作も、現在のひらがなと同じにしてください。終了後に一覧へ戻す場合は、選択していた文字種の一覧へ戻してください。

## 文字データ

ひらがなとカタカナで共通の文字型を使用してください。

例：

```ts
type CharacterScript = 'hiragana' | 'katakana'
```

各文字から文字種を判別できるようにしてください。

既存の型やプロパティ名がある場合は、それに合わせてください。

## IDの管理

ひらがなとカタカナの学習記録が混ざらないよう、文字IDを重複させないでください。

既存のひらがなIDと学習記録を維持することを優先してください。

推奨例：

```text
ひらがな「あ」：a
カタカナ「ア」：katakana-a
```

すでにカタカナ用のIDが存在する場合は、重複や不整合がないか確認し、利用可能ならそのまま使用してください。

## 学習記録

「ひらがな」と「カタカナ」は別の学習記録として保存してください。

例：

- 「あ」を5回書いても「ア」の回数は増えない
- 「ア」を習得済みにしても「あ」の状態は変わらない

それぞれの文字について、現在の仕様に合わせて次を保持してください。

- 学習回数
- 書いた回数
- 学習ステータス
- 手動の習得済み状態

## 学習ステータス

現在のひらがなの判定を、カタカナにも同じように適用してください。

- 未学習：書いた回数が0回で、習得済みに設定されていない
- 学習中：書いた回数が1回以上で、習得済みに設定されていない
- 習得済み：利用者が習得済みに設定した文字

ひらがなとカタカナで独立して判定してください。

## LocalStorageの移行

既存のひらがな学習記録を消さないでください。

- データ構造のバージョンを確認する
- 必要な場合だけバージョンを更新する
- 既存のひらがな記録をそのまま引き継ぐ
- カタカナ46文字の不足データを追加する
- すでに保存されているカタカナデータがあれば可能な限り引き継ぐ
- データ移行を理由に記録全体を初期化しない
- 学習記録リセットでは両方の記録をリセットする

## 戻る操作

画面遷移に応じて、戻る操作が自然に動作するようにしてください。

```text
カタカナ書き練習
  ↓ 戻る
カタカナ文字一覧
  ↓ 戻る
文字種選択画面
  ↓ 戻る
ホーム画面
```

ひらがなについても同じ階層にしてください。

ブラウザの戻るボタンでも正しく戻れるようにしてください。

## URLと再読み込み

直接URLを開いた場合やページを再読み込みした場合も、選択した文字種と文字を復元できるようにしてください。

可能であれば、文字種と文字IDをURLで表現してください。

例：

```text
/learn
/learn/hiragana
/learn/katakana
/study/hiragana/a
/study/katakana/katakana-a
```

実際のURLは、現在のルーティング構造に合うものを選んでください。

一時的な`location.state`だけに依存し、再読み込みすると選択内容が失われる設計は避けてください。

## 主に確認するファイル

- @src/App.tsx
- @src/pages/HomePage.tsx
- @src/pages/StudyPage.tsx
- @src/pages/ProgressPage.tsx
- @src/data/hiragana.ts
- @src/types/index.ts
- @src/store/LearningContext.tsx
- @src/services/storage.ts
- @README.md

すでに追加されているカタカナ関連のページ、データ、コンポーネントもすべて確認してください。

## UI条件

- ひらがなとカタカナで同じデザインを使用する
- 文字一覧を五十音表形式で表示する
- 文字を大きく表示する
- タップ領域を十分に確保する
- 色だけで学習ステータスを表現しない
- 選択中の文字種を画面タイトルに表示する
- Androidの幅360pxで横スクロールを発生させない
- 既存の明るく親しみやすいデザインを維持する

## 変更しないもの

- 既存のひらがな学習機能
- Canvasの描画方法
- 5回書くという条件
- 「次へ」の基本動作
- 手動の習得済み機能
- PWA機能
- 音声・テスト機能の現在の状態

削除済みの音声機能やテスト機能を再追加しないでください。

## READMEの更新

READMEに次を反映してください。

- 学習モードでひらがなとカタカナを選択できる
- 文字種を選んだ後、文字一覧から練習したい文字を選ぶ
- カタカナは清音46文字に対応している
- ひらがなとカタカナの学習記録は別々に保存される
- 濁音、半濁音、拗音は今回の書き練習対象に含まれない

## 完成条件

- 学習モードを押すと文字種選択画面が表示される
- 「ひらがな」を押すとひらがな文字一覧が表示される
- 「カタカナ」を押すとカタカナ文字一覧が表示される
- 文字種を選んだだけでは書き練習を開始しない
- 一覧の文字をタップすると書き練習が始まる
- カタカナの画面と機能がひらがなと同じ構成になっている
- カタカナ46文字が正しい五十音順で表示される
- 「次へ」で同じ文字種の次の文字へ進む
- 終了後は正しい文字種の一覧へ戻る
- ひらがなとカタカナの記録が混ざらない
- 既存のひらがな記録が消えない
- 再読み込み後も選択状態を復元できる
- PCとAndroidで書き練習が動作する
- 幅360pxでレイアウトが崩れない
- TypeScriptとビルドのエラーがない
- 未使用importやLintエラーがない

最初に、現在のひらがな学習モードの画面遷移、文字一覧、書き練習、学習記録の仕組みを調査してください。

その後、現在のカタカナ実装との違い、修正対象、共通化する処理、LocalStorageへの影響を説明し、そのまま実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

さらに次を確認してください。

- ひらがな46文字
- カタカナ46文字
- 全文字IDの重複がない
- ひらがなの既存IDが維持されている
- カタカナの記録がひらがなへ加算されない
- URLを直接開いても正しい文字種を表示できる

最後に次を報告してください。

- 修正した既存のカタカナ実装
- 追加・変更・削除したファイル
- 文字種選択から書き練習までの画面遷移
- ひらがなと共通化した処理
- 文字IDの管理方法
- LocalStorageの移行方法
- ビルドとLintの結果
- Android実機で確認すべき項目