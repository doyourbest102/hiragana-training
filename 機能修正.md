書き練習モードの「完了」ボタンを「次へ」ボタンに変更し、押したら確認画面を表示せず、ひらがなの次の文字へ進むように修正してください。

## 変更前

1. 同じひらがなを5回書く
2. 「完了」ボタンを押す
3. 「もう一度する」または「次の文字」を選ぶ画面が表示される
4. 利用者が次の操作を選択する

## 変更後

1. 同じひらがなを5回書く
2. 「次へ」ボタンが押せるようになる
3. 「次へ」を押す
4. 確認画面を表示しない
5. 現在の文字の学習記録を保存する
6. 五十音順の次のひらがなへ移る
7. Canvasと練習回数をリセットする

例：

```text
あ → い → う → え → お → か → き → …
```

## 関連ファイル

- @src/pages/StudyPage.tsx
- @src/components/WritingCanvas.tsx
- @src/data/hiragana.ts
- @src/store/LearningContext.tsx

必要であれば、これらから参照されているファイルも確認してください。

## ボタンの仕様

- ボタンの表示を「完了」から「次へ」に変更する
- 5回書くまでは「次へ」を無効にする
- 5回書いたら「次へ」を有効にする
- 有効・無効の状態を色だけで表現しない
- Androidスマートフォンで押しやすい48px以上の高さを維持する
- 連続タップによる二重処理を防止する

## 「次へ」を押したときの処理

次の順番で処理してください。

1. 現在の文字の学習記録を1回だけ保存する
2. 現在の文字の書いた回数を正しく加算する
3. 次の文字へ切り替える
4. 練習回数を0回に戻す
5. Canvasの手書き線を消す
6. 次の文字の見本を表示する
7. 進行状況を更新する
8. 「次へ」ボタンを再び無効にする

「もう一度する／次の文字」を選択する中間画面は表示しないでください。

## 文字の順番

@src/data/hiragana.tsで管理されている文字の順番を使用してください。

順番を別の場所へ重複して定義しないでください。

文字を直接決め打ちするのではなく、現在の配列の次の要素へ進む実装にしてください。今後、濁音などが追加されても変更しやすい構造を維持してください。

## 最後の文字

最後の「ん」では次の文字が存在しないため、ボタンを次のようにしてください。

```text
学習を完了する
```

最後の文字を5回書いてボタンを押した場合だけ、学習セッション全体の完了画面を表示してください。

各文字の途中では完了画面や確認画面を表示しないでください。

## 1文字だけを練習している場合

学習記録画面から特定の1文字だけを選んで練習している場合は、対象文字を5回書いた後のボタンを次のようにしてください。

```text
練習を完了する
```

押した後は、次の文字へ移動せず、元の学習記録画面へ戻してください。

単一文字練習と通常の連続練習を区別してください。

## 削除するもの

各文字の途中で使用している次の処理があれば削除してください。

- 「もう一度する」ボタン
- 「次の文字」を選択する確認画面
- 中間完了画面
- 中間完了画面を表示するためだけのstate
- 使用されなくなった関数
- 使用されなくなったimport
- 使用されなくなったCSS

最後の文字に到達した後の、学習全体の完了画面は残してください。

## 変更しないもの

- 5回書くという条件
- Canvasの描画処理
- 学習回数と書いた回数の保存
- 学習ステータス
- LocalStorageのデータ構造
- 五十音データ
- PWA設定
- 他の画面のデザイン

## 完成条件

- 5回書くまでは「次へ」を押せない
- 5回書くと「次へ」を押せる
- 「次へ」を押すと確認画面なしで次の文字へ進む
- 五十音順で正しい次の文字が表示される
- Canvasが空の状態になる
- 練習回数が0回に戻る
- 現在の文字の記録が1回だけ保存される
- ボタンの連続タップで文字を飛ばさない
- 最後の文字では「学習を完了する」と表示される
- 単一文字練習では「練習を完了する」と表示される
- PCとAndroidの両方で動作する
- 幅360pxでレイアウトが崩れない
- TypeScriptとビルドのエラーがない
- 未使用importやLintエラーがない

最初に現在の画面遷移と、中間確認画面を表示している処理を確認してください。その後、不要な画面と状態を削除し、最小限の差分で実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

最後に次を報告してください。

- 変更したファイル
- 削除した確認画面と状態
- 次の文字を決定する方法
- 学習記録の二重保存を防ぐ方法
- 最後の文字の動作
- 単一文字練習の動作
- ビルドとLintの結果