Androidスマートフォンで、勉強モードのCanvasに指で文字を書けない問題を調査し、修正してください。

## 現在の状況

- PCではマウスを使って正常に書ける
- AndroidスマートフォンまたはiPhoneでは指で書けない、または線が正しい位置に表示されない
- スマートフォンの縦画面で使用している
- 既存のデザインや学習機能は維持したい

## 関連ファイル

- @src/components/WritingCanvas.tsx
- @src/pages/StudyPage.tsx
- @src/styles/index.css

必要であれば、これらから参照されているファイルも調査してください。

## 調査してほしい点

原因を決めつけず、現在の実装を読んで次の点を確認してください。

1. ReactのPointer EventsがAndroid Chromeで正しく開始・移動・終了しているか
2. `setPointerCapture`、`pointercancel`、`lostpointercapture`の処理に問題がないか
3. CSSの`touch-action`やブラウザのスクロール処理が描画を妨げていないか
4. CanvasのCSS表示サイズと内部解像度にずれがないか
5. `devicePixelRatio`を二重に適用していないか
6. `getBoundingClientRect()`から取得した座標とCanvas描画座標が一致しているか
7. 画面回転、リサイズ、アドレスバーの表示変化でCanvasが意図せず初期化されていないか
8. Android Chromeの通常表示とPWA表示の両方で問題になりそうな処理がないか

特に、高DPIのスマートフォンで、Canvasの内部サイズを`devicePixelRatio`倍した後にContextへスケールを設定し、さらに内部の`width`と`height`を描画座標として使っている部分を確認してください。座標や描画倍率が二重に拡大されていないか検証してください。

## 修正条件

- 1本の指で自然に線を書ける
- 線が指の位置に追従する
- Canvas上で書いている間だけ画面がスクロールしない
- Canvas外では通常どおり画面をスクロールできる
- 指をCanvas外へ動かして離した場合も描画状態が正しく終了する
- 「書き直す」ボタンが従来どおり動作する
- 練習回数のカウントを壊さない
- PCのマウス描画も引き続き使用できる
- 幅360pxの画面でレイアウトを崩さない
- 関係のない画面、デザイン、学習データは変更しない
- タッチイベントとマウスイベントを安易に重複実装せず、可能ならPointer Eventsで統一する
- 最小限で保守しやすい修正にする

最初に原因と修正方針を簡潔に説明し、そのまま実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

最後に、次の内容を報告してください。

- 原因
- 変更したファイル
- 変更内容
- PCでの確認結果
- Android Chrome実機で確認すべき操作手順
- 自動確認できなかった項目