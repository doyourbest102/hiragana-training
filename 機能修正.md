ひらがなの「あ」を再生したとき、Androidスマートフォンで「ぱ」のように聞こえる問題を調査し、正しい「あ」の発音に聞こえるよう修正してください。

## 現在の問題

- 画面に表示される文字は「あ」で正しい
- 音声再生ボタンを押すと「あ」ではなく「ぱ」のように聞こえる
- Web Speech APIの`SpeechSynthesisUtterance`を使用している
- ゆっくりした速度で音声を再生している
- Android ChromeおよびPWAで発生している

## 関連ファイル

- @src/hooks/useSpeech.ts
- @src/data/hiragana.ts
- @src/pages/StudyPage.tsx
- @src/pages/TestPage.tsx
- @src/components/SoundButton.tsx

必要であれば、これらから参照されているファイルも調査してください。

## 最初に確認すること

1. 「あ」の`speechText`が正しく「あ」になっているか
2. `SpeechSynthesisUtterance`へ実際に渡される文字列が「あ」になっているか
3. 直前に再生した音声が残ったり、重なったりしていないか
4. `speechSynthesis.cancel()`直後の再生によって、音声の先頭が欠けていないか
5. 選択されている音声の`name`、`lang`、`localService`を確認する
6. `ja-JP`以外の音声が選択されていないか
7. 再生速度を遅くしすぎたことで発音が不自然になっていないか
8. 1文字だけの非常に短い発話がAndroidの音声エンジンで誤って処理されていないか
9. 勉強モードとテストモードで同じ問題が発生するか
10. 通常のChrome表示とPWA表示で違いがあるか

調査用ログを一時的に追加して構いませんが、修正後は不要なログを削除してください。

## 修正方針

- 表示する文字やテストの正解は変更しない
- `hiragana`、`romaji`、`speechText`の役割を混同しない
- ローマ字の`a`を読み上げさせない
- 日本語の`ja-JP`音声を確実に優先する
- 特定端末の音声名だけを固定する実装は避ける
- 音声の先頭が欠けないよう、キャンセルと再生の競合を防ぐ
- 現在の速度で発音が崩れる場合は、明瞭さを優先して`rate`を少し調整する
- 極端に遅い速度にはしない
- 関係のない画面や学習記録は変更しない

1文字の「あ」がAndroidの音声エンジンで不安定な場合は、読み上げ専用テキストとして、次の候補を実機で比較できる構造にしてください。

- `あ。`
- `あ、`
- `あー。`

最初は発音内容をほとんど変えない`あ。`を優先してください。それで明瞭にならない場合だけ、学習用として自然に聞こえる候補へ変更してください。

「あ」だけの特別処理を音声フック内に直接書くのではなく、必要なら@src/data/hiragana.tsの`speechText`で管理してください。今後、ほかの文字の発音も個別に調整できる構造を維持してください。

## 完成条件

- 「あ」が日本語の母音「あ」として明瞭に聞こえる
- 「ぱ」「は」など別の音に聞こえにくい
- 音声の先頭が欠けない
- 勉強モードとテストモードの両方で正しく再生される
- 手動再生と自動再生で同じ発音になる
- 連続して再生しても別の音が混ざらない
- 「い」「う」「え」「お」など、ほかの文字の発音を壊さない
- PCでの音声再生も維持する
- Android ChromeとPWAの両方で確認できる
- TypeScriptの型エラーとビルドエラーがない

最初に考えられる原因と採用する修正方法を説明し、そのまま最小限の差分で実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

最後に次を報告してください。

- 実際に読み上げへ渡している文字列
- 選択される日本語音声の条件
- 採用した再生速度
- 音声の先頭欠けを防ぐ方法
- 変更したファイル
- 「あ」以外への影響
- Android Chrome実機で比較する手順
- 実機でなければ確認できない項目

Android実機で確認していない場合は、「修正済み」と断定せず、コード上で改善した内容と実機確認が必要な点を明記してください。