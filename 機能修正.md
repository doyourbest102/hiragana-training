ひらがなの音声を、初級学習者ができるだけはっきり聞き取れるように、常にゆっくりした速度で再生するよう修正してください。

## 現在の実装

- Web Speech APIの`SpeechSynthesisUtterance`を使用している
- @src/hooks/useSpeech.ts で音声速度を設定している
- 文字データの`speechText`は「あ」「き」などの1文字になっている
- 勉強モードとテストモードで同じ音声機能を使用している
- Android ChromeおよびPWAでの利用を想定している

## 関連ファイル

- @src/hooks/useSpeech.ts
- @src/pages/StudyPage.tsx
- @src/pages/TestPage.tsx
- @src/data/hiragana.ts

必要であれば、これらから参照されているファイルも調査してください。

## 実現したいこと

- すべてのひらがな音声を、常にゆっくり再生する
- 利用者が速度を選択する機能は追加しない
- 速度設定用のボタンや画面は追加しない
- 初級学習者が1音ずつ聞き取りやすい自然な発音にする

音声速度は`rate = 0.65〜0.7`を目安にしてください。ただし、速度を下げすぎて発音が不自然になる場合は、Androidの日本語音声で明瞭に聞こえる範囲を優先して調整してください。

## 調査・実装してほしい点

1. 現在の音声速度が実際の再生処理に確実に適用されているか確認する
2. `SpeechSynthesisUtterance`を作成するたびに同じ速度を設定する
3. 日本語音声を優先して選択する既存処理を維持する
4. `lang`は`ja-JP`、`pitch`は基本的に`1`を維持する
5. 勉強モードとテストモードの両方に同じ速度を適用する
6. 自動再生と手動再生の両方に同じ速度を適用する
7. 1文字だけの発話が途中で切れたり、不明瞭になったりしないか確認する
8. 必要であれば、発音内容を変えない範囲で句点や短い間を利用し、短い音声が切れにくくなるようにする
9. 「は」「へ」「を」などが助詞として不自然に読まれる可能性を確認する
10. 連続再生や再生し直しでも速度が変わらないようにする

## 修正条件

- 音声速度は1種類の「ゆっくり」のみにする
- 速度選択UIや設定保存機能は追加しない
- 外部音声サービスや音声ファイルは追加しない
- Android Chromeで手動再生できる
- PWAとして起動した場合も同じ速度で再生される
- PCで現在動いている音声再生を壊さない
- 学習記録やテストの正誤判定に影響を与えない
- 関係のない画面やデザインは変更しない
- 最小限で保守しやすい修正にする
- TypeScriptの型エラーとビルドエラーがない

最初に現在の音声処理を確認し、採用する速度と理由を説明してください。その後、そのまま最小限の差分で実装してください。

実装後は以下を実行してください。

npm.cmd run build
npm.cmd run lint

最後に次を報告してください。

- 採用した再生速度
- その速度を選んだ理由
- 変更したファイル
- 1文字の発音を明瞭にするために行った処理
- PCでの確認結果
- Android Chrome実機で確認する手順
- 実機でなければ確認できない項目